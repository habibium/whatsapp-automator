# web

The browser app: Vite + React with TanStack Router, Query and Form, styled with Astryx components and StyleX. In production the Rust server serves the built files from `web/dist` on the same port as the API. In development Vite proxies `/api` to the server on `localhost:8000`.

Run everything with `just dev` from the repository root, or this app alone:

```sh
pnpm install
pnpm dev
```

## Commands

| Script              | What it does                                                                   |
| ------------------- | ------------------------------------------------------------------------------ |
| `pnpm dev`          | Vite dev server on http://localhost:5173 with `/api` proxied to the backend    |
| `pnpm build`        | Production build into `dist/`                                                  |
| `pnpm preview`      | Serve the production build locally                                             |
| `pnpm typecheck`    | `tsc --noEmit`                                                                 |
| `pnpm lint`         | oxlint                                                                         |
| `pnpm check`        | oxfmt formatting check                                                         |
| `pnpm format`       | Format with oxfmt and apply oxlint fixes                                       |
| `pnpm gen:api`      | Regenerate `src/api/schema.d.ts` from `../openapi.json`; `--check` verifies it |
| `pnpm astryx <cmd>` | Astryx CLI: component docs, search, templates                                  |

## API client

`src/api/client.ts` exports `api` (openapi-fetch) and `$api` (openapi-react-query), both typed from `src/api/schema.d.ts`. That file is generated from the backend's `openapi.json` and never edited by hand: `pnpm gen:api` regenerates it, the Vite plugin in `vite.config.ts` does the same whenever the spec changes during `just dev`, and CI fails if it is stale.

## Styling

This project uses [Astryx](https://github.com/facebook/astryx) (`@astryxdesign/*`) for components
and [StyleX](https://stylexjs.com/) for custom styling. There is no Tailwind, no CSS modules and no
`className` utilities.

- `src/styles.css` imports the three Astryx stylesheets in order — `@astryxdesign/core/reset.css`
  (`@layer reset`), `@astryxdesign/core/astryx.css` (`@layer astryx-base`) and
  `@astryxdesign/theme-neutral/theme.css` (`@layer astryx-theme`) — plus the Figtree weights the
  theme asks for. It is imported once from `src/main.tsx`; add nothing else to it.
- The theme is wired in `src/routes/__root.tsx` with `<Theme theme={neutralTheme}>`, and
  `<LinkProvider component={Link}>` routes every Astryx link through TanStack Router.
- Components come from per-name subpaths: `@astryxdesign/core/Button`, `/TextInput`, `/Card`,
  `/VStack`, `/Center`, `/Text` (also exports `Heading`), `/Link`, `/theme`.
- Custom styling is `stylex.create` passed to a component's `xstyle` prop; `stylex.props` only on
  the rare DOM node Astryx has no component for. Values come from
  `@astryxdesign/core/theme/tokens.stylex` or `var(--token)`, never raw hex or px.
- The StyleX Vite plugin runs with `useCSSLayers: false`, so product styles stay **unlayered** and
  therefore beat every Astryx layer regardless of source order. Do not add other unlayered global
  CSS — it would beat the design system too.

The Astryx CLI is the reference for any component or prop:

```bash
pnpm astryx component Button    # props + examples
pnpm astryx search "date range"
pnpm astryx docs tokens         # spacing, color, radius reference
pnpm astryx doctor              # health check
```

## Routing

TanStack Router with file-based routes in `src/routes`. `src/routeTree.gen.ts` is generated by the router plugin and must not be edited. Because `<LinkProvider component={Link}>` is wired in the root route, navigate with Astryx's `<Link href="/x">` or `<Button href="/x">` rather than a raw `<a>`.

## Conventions

The rules for this app (StyleX only, token values, layout components before raw elements, forms with TanStack Form + zod) are in the repository's [AGENTS.md](../AGENTS.md).
